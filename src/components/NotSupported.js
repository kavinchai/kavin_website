import React from "react";
import "../css/NotSupported.css";

const NotSupported = () => {
  return (
    <div className="notSupContainer">
      <div className="notSup">
        Sorry, this window resolution is not supported.
      </div>
    </div>
  );
};

export default NotSupported;
